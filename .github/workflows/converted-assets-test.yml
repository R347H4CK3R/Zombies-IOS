name: Converted Assets Tests

on:
  push:
    branches: [ converted-tranzit-assets ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run converted asset architecture tests
        run: python3 -m unittest Tools.tests.test_converted_asset_architecture -v
